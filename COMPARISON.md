# A / B / C の比較

同じ音声エージェントを 3 通りで作り、**遅延・費用・正しさ**で比べた記録。
数字の出どころは `results/runs.csv`（全行）と `results/raw/*.json`（API の生の応答）。

最終更新: 2026-09-11 / LLM は 3 構成とも `gemini-3.6-flash`

## 3 つの構成

違いは「**何を ElevenLabs に渡し、何を手元に置くか**」だけ。

| | A: Agents Platform | B: 自前構成 | C: Knowledge Base |
|---|---|---|---|
| ノートの本文 | 手元 | 手元 | **ElevenLabs に預ける** |
| 検索の実装 | 手元（`search.py`） | 手元（`search.py`） | ElevenLabs 内部の RAG |
| 検索を呼ぶ判断 | Agent がする | **こちらが必ず先に呼ぶ** | Agent がする |
| 応答生成（LLM） | ElevenLabs 内部 | 手元から Gemini API | ElevenLabs 内部 |
| 音声認識・発話・割り込み | ElevenLabs | 自分（今はテキスト入力で回避） | ElevenLabs |
| 公開 URL / トンネル | 不要（client tool） | 不要 | 不要 |

## 揃えた条件と、揃っていない条件

**揃えたもの**: 同じ 5 問、同じ声（Jessica）、同じ TTS モデル（`eleven_flash_v2_5`）、
同じ LLM 名、同じ指示文（道具の有無に関する行だけ差し替え）、同じ数字の正規化。

**揃っていないもの**:

- **C だけ検索が別物**。A・B は文字バイグラム、C は ElevenLabs の多言語埋め込み。
  C の答えが違ったとき、それは音声の経路ではなく**検索の差**。ただし C の売りが
  「検索も任せる」ことなので、切り離して測ること自体ができない
- **B だけ LLM の経路が違う**。公開 API 経由の Gemini を手元から呼ぶ。
  A・C は ElevenLabs の中から同じモデルを呼ぶ

## 結果

C は実行待ち。

| 指標 | A: Agents Platform | B: 自前構成 | C: Knowledge Base |
|---|---:|---:|---:|
| 往復数 | 7 | 6 | 実行待ち |
| 最初の音まで（中央値） | **2,061 ms** | 3,543 ms | |
| 最速 | 1,871 ms | 1,674 ms | |
| 最遅 | **2,653 ms** | 6,224 ms | |
| ばらつき（最遅 − 最速） | **782 ms** | 4,550 ms | |
| 言い終わりまで（中央値） | 2,732 ms | 3,984 ms | |
| 1 往復あたりのクレジット | 91 | **46** | |
| 課金の単位 | 通話時間（分） | 合成した文字数 | 通話時間（分） |
| ほかに費用がかかる先 | なし | Gemini のトークン | なし |
| 正答 | 5/5 | 5/5 | |

### 質問ごと（最初の音まで ms / クレジット）

| 質問 | A | B | C |
|---|---:|---:|---:|
| デプロイ前の確認 | 2,479 / 100 | 3,460 / 55 | |
| ロールバック | 2,653 / 100 | 6,224 / 50 | |
| RRF とは | 2,633 / 98 | 4,229 / 50 | |
| 5/12 の定例 | 2,061 / 69 | 3,626 / 52 | |
| 来週の天気（ノートに無い） | 2,049 / 85 | 1,674 / 16 | |

## 内訳（生の JSON から）

A の 1 往復 2.1 秒の中身。`results/raw/*.json` の `conversation_turn_metrics` にある。

| 段階 | 時間 |
|---|---:|
| LLM が「道具を呼ぶ」と判断するまで | 0.71 s |
| 道具の結果を受けて最初の文を書くまで | 0.88 s |
| 音声合成が最初の音を出すまで | 0.10 s |

B の内訳（`results/transcripts/*_custom_*.txt`）は、検索 1〜2 ms、
Gemini の最初のトークンまで 1.5〜5.9 s、TTS へ送ってから音が出るまで 200〜300 ms。

**どちらも遅延のほとんどは LLM**。音声合成は 0.1〜0.3 秒しか使っていない。

## どの組み合わせが何を語るか

- **A と C**: 設定が同じで違いは検索の置き場所だけ。**ツール往復の代償**が測れる、
  一番きれいな比較
- **A と B**: 検索は同じだが LLM の呼び方が違う。B の遅さは「自前だから」ではなく
  「公開 API 経由の Gemini だから」。自前の部分（検索 1〜2 ms、TTS 0.2〜0.3 s）は速い
- **B 単体**: 唯一、内訳が全部見える

## 今言えること

- **A は速く、ばらつきが小さい**（1.9〜2.7 秒に収まる）。B は最悪 6.2 秒
- **B は安い**（1 往復 46 対 91）。ただし Gemini のトークンが別勘定で、請求先が 2 社に分かれる
- **課金の単位の違いが効く場面がある**。A は通話時間で課金されるので**黙っていても減る**。
  6 秒話して 24 秒黙る会話なら A は約 360 クレジット、B は約 46。
  実際、放置された 5 分の会話 1 本で 4,168 クレジット（Starter の 14%）が消えた
- **同じモデル名でも、誰がどう呼ぶかで 1〜3 秒変わる**。これが一番大きい発見

## まだ測っていないこと

割り込みの品質、電話、同時接続数、長時間の安定性、音声入力（Scribe）を通したときの遅延。
今の B はテキスト入力なので、**音声認識の時間は 3 構成とも含まれていない**。

## 生データ

| 置き場所 | 中身 |
|---|---|
| `results/runs.csv` | 1 往復 1 行。追記なので過去も全部残る |
| `results/report.md` | 上の CSV から機械生成した表 |
| `results/raw/*.json` | ElevenLabs API の応答そのまま（ターンごとの内訳、道具の引数と戻り値、LLM のトークンと単価） |
| `results/transcripts/*.txt` | 返答の全文と時刻の内訳 |
| `results/audio/*.wav` | 録音（git には入れない） |
